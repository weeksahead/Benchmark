# Monday.com Integration (Server-side only - DO NOT use NEXT_PUBLIC_ prefix)
MONDAY_API_TOKEN=your_monday_api_token
MONDAY_BOARD_ID=9864313431  # Contact form board (used by /api/contact)
# Note: Equipment ROI Calculator uses board 9864593426 (hardcoded in /api/roi-calculation-*)

# Claude AI Chat (Server-side only - DO NOT use NEXT_PUBLIC_ prefix)
CLAUDE_API_KEY=your_claude_api_key

# GitHub Admin Integration (for saving changes)
GITHUB_TOKEN=your_github_personal_access_token
# To get a GitHub token:
# 1. Go to GitHub Settings > Developer Settings > Personal Access Tokens
# 2. Click "Generate new token (classic)"
# 3. Give it a name like "Benchmark Admin"
# 4. Select "repo" scope
# 5. Copy the token and add it to Vercel environment variables